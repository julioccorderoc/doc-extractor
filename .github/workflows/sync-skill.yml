name: Sync SKILL.md to agent-skills registry

on:
  push:
    branches: [main]
    paths: [SKILL.md]

jobs:
  open-sync-pr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Clone agent-skills fork and update
        env:
          GH_TOKEN: ${{ secrets.AGENT_SKILLS_PAT }}
        run: |
          BRANCH="sync-doc-extractor-$(date +%Y%m%d)"

          # Clone your fork of vercel-labs/agent-skills
          git clone https://x-access-token:${GH_TOKEN}@github.com/julioccorderoc/agent-skills.git
          cd agent-skills

          # Create or reset the sync branch from main
          git checkout -B "$BRANCH"

          # Create or update the skill file
          mkdir -p skills/doc-extractor
          cp ../SKILL.md skills/doc-extractor/SKILL.md

          # Commit and force-push (safe: this is our own fork's ephemeral sync branch)
          git add skills/doc-extractor/SKILL.md
          git commit -m "sync: update doc-extractor SKILL.md from source repo"
          git push origin HEAD --force

          # Open PR — silently skip if one already exists for this branch
          gh pr create \
            --repo vercel-labs/agent-skills \
            --title "sync: update doc-extractor SKILL.md" \
            --body "Automated sync from https://github.com/julioccorderoc/doc-extractor" \
            --head "julioccorderoc:${BRANCH}" \
            || echo "PR already open for ${BRANCH} — branch updated in place."
