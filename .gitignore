# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# local files/folders
# NOTE: docs/ and tests/ are TRACKED and stay tracked. They were listed here
# once, which had no effect on already-tracked files and left the ignore file
# asserting the opposite of the repo's actual contents. The real concern was
# never documentation — it was test_docs/, which holds real business documents.
test_docs/
.ai/
.DS_Store
evals/
eng.traineddata
.github/workflows/
experiments/
fix_snapshot_timeout.sh
.desloppify/
.claude/
scorecard.png
.github/
.pytest_cache/
.ruff_cache/
